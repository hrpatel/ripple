import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Hello, Ripple!")
                .font(.headline)
        }
        .frame(width: 320, height: 400)
    }
}
